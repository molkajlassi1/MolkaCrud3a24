{% extends 'base.html.twig' %}

{% block title %}New Student{% endblock %}

{% block body %}
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header">
                    <h1 class="my-2">Create new Student</h1>
                </div>
                <div class="card-body">
                    {{ include('student/_form.html.twig') }}
                </div>
                <div class="card-footer text-center">
                    <a href="{{ path('app_student_index') }}" class="btn btn-secondary btn-lg mt-3">Back to list</a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
